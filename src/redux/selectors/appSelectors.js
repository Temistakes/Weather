export function getTemp(state) {
    return state.app.temp;
}

export function getHumidity(state) {
    return state.app.humidity;
}

export function getWindSpeed(state) {
    return state.app.windSpeed;
}

export function getWeather(state) {
    return state.app.weather;
}

export function getCity(state) {
    return state.app.city;
}

export function getLoaded(state) {
    return state.app.loaded;
}